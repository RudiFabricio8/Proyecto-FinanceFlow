ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        host = "0.0.0.0"
    }
    application {
        modules = [ com.financeflow.ApplicationKt.module ]
    }
}

jwt {
    secret = "financeflow-secret-key-change-me-to-something-very-secure"
    issuer = "http://0.0.0.0:8080/"
    audience = "http://0.0.0.0:8080/api"
    realm = "Access to FinanceFlow"
    expiration = 86400000 // 24 hours
}

db {
    driver = "org.postgresql.Driver"
    jdbcUrl = "jdbc:postgresql://localhost:5432/financeflow"
    user = "postgres"
    password = "RFMaJa775"
}